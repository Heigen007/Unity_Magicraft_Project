# Stage 13: АНИМИРОВАННЫЕ ВРАГИ СО СПРАЙТАМИ

**Цель**: Заменить простые кружки врагов на полноценные анимированные спрайты с Walk анимацией

---

## 📁 Исходные данные

### Расположение спрайтов:
```
Animations/enemies/1/Walk.png  (6 кадров)
Animations/enemies/2/Walk.png  (6 кадров)
Animations/enemies/3/Walk.png  (6 кадров)
```

### Типы врагов:
- **Enemy Type 1** - базовый враг (используем папку 1/)
- **Enemy Type 2** - средний враг (используем папку 2/)

---

## 🎯 ЭТАП 1: Подготовка спрайтов в Unity

### 1.1 Настройка Walk.png для Enemy 1

**ШАГ 1: Открой файл**
1. В Unity внизу окно **Project** (там где файлы)
2. Открой папку: `Assets` → `project` → `Animations` → `enemies` → `1`
3. **Кликни ОДИН РАЗ** на файл `Walk.png` (НЕ двойной клик!)

**ШАГ 2: Настрой как спрайт**

Справа появится окно **Inspector**. Делай по порядку:

1. **Texture Type** → выбери `Sprite (2D and UI)`
2. **Sprite Mode** → выбери `Multiple` (много картинок в одном файле)
3. **Pixels Per Unit** → поставь `32`
4. **Filter Mode** → выбери `Point (no filter)`
5. **Compression** → выбери `None`
6. **НАЖМИ КНОПКУ APPLY ВНИЗУ!**

**ШАГ 3: Нарежь на кадры**

1. В **Inspector** появилась кнопка **Sprite Editor** → **кликни**
2. Откроется новое окно
3. Вверху найди кнопку **Slice** → кликни
4. Появится меню справа:
   - **Type**: `Grid By Cell Count`
   - **Column**: `6` (шесть кадров)
   - **Row**: `1` (одна строка)
   - **Нажми кнопку Slice**
5. Картинка разделится на 6 прямоугольников (это кадры анимации)
6. **Закрой окно Sprite Editor** (крестик или Apply → Close)
7. Unity спросит "Apply changes?" → **Apply**

**ШАГ 4: Проверь результат**

1. В окне **Project** кликни на **стрелочку** (треугольник) рядом с `Walk.png`
2. Должно раскрыться **6 маленьких картинок**:
   - `Walk_0`
   - `Walk_1`
   - `Walk_2`
   - `Walk_3`
   - `Walk_4`
   - `Walk_5`

✅ **Если видишь эти 6 картинок - готово!**

---

### 1.2 Настройка Walk.png для Enemy 2

**Повтори ШАГ 1-4 для второго врага:**
1. Файл: `Animations/enemies/2/Walk.png`
2. Те же настройки (Sprite Mode: Multiple, Column: 6, Row: 1)
3. Проверь что появилось 6 картинок: `Walk_0` до `Walk_5`

---

## 🎯 ЭТАП 2: Создание анимаций врагов

### 2.1 Создай анимацию Enemy1_Walk

**ШАГ 1: Создай файл анимации**

1. В окне **Project** открой папку `Animations`
2. **ПКМ (правая кнопка мыши)** → **Create** → **Animation**
3. Назови файл: `Enemy1_Walk`
4. Нажми Enter

**ШАГ 2: Открой окно анимации**

1. Вверху Unity: **Window** → **Animation** → **Animation**
2. Откроется окно **Animation** (обычно внизу)
3. В этом окне **кликни на дропдаун** (где написано название анимации)
4. Выбери `Enemy1_Walk` (файл который создал)

**ШАГ 3: Добавь кадры**

1. В окне **Project** найди `enemies/1/Walk.png`
2. **Кликни на стрелочку** → раскроются 6 спрайтов
3. **Выделi ВСЕ 6 спрайтов** (кликни на первый, зажми Shift, кликни на последний):
   - `Walk_0`
   - `Walk_1`
   - `Walk_2`
   - `Walk_3`
   - `Walk_4`
   - `Walk_5`
4. **Перетащи их** (drag & drop) в окно **Animation** на Timeline (на линию времени)
5. Появятся кадры анимации

**ШАГ 4: Настрой скорость**

1. В окне **Animation** найди поле **Samples** (обычно слева сверху)
2. Поставь значение `8` (это 8 кадров в секунду)
3. Убедись что галочка **Loop** стоит (чтобы анимация зациклилась)

**ШАГ 5: Сохрани**

1. **Ctrl+S** или просто закрой окно Animation
2. Готово!

---

### 2.2 Создай анимацию Enemy2_Walk

**Повтори ШАГ 1-5 для второго врага:**
1. Создай файл: `Enemy2_Walk`
2. Перетащи спрайты из `enemies/2/Walk.png` (все 6 штук)
3. Samples: `8`
4. Loop: ✓
5. Сохрани

---

## 🎯 ЭТАП 3: Создание Animator Controllers

### 3.1 Создай Enemy1_AnimatorController

**ШАГ 1: Создай контроллер**

1. В окне **Project** открой папку `Animations`
2. **ПКМ** → **Create** → **Animator Controller**
3. Назови: `Enemy1_AnimatorController`
4. Нажми Enter

**ШАГ 2: Открой Animator**

1. **Двойной клик** на `Enemy1_AnimatorController`
2. Откроется окно **Animator** (большое, с сеткой)

**ШАГ 3: Создай состояние Walk**

1. В окне **Animator** кликни **ПКМ** (на пустом месте в сетке)
2. **Create State** → **Empty**
3. Появится оранжевый прямоугольник с названием "New State"
4. **Кликни на него один раз** (выделится)
5. Справа в **Inspector**:
   - **Name**: напиши `Walk`
   - **Motion**: перетащи файл `Enemy1_Walk` (анимацию) сюда
   - Или кликни на кружочек справа от Motion → выбери `Enemy1_Walk`

**ШАГ 4: Сделай Walk стартовым состоянием**

1. Кликни **ПКМ** на состоянии `Walk`
2. Выбери **Set as Layer Default State**
3. Прямоугольник станет **оранжевым** (это значит "стартовое")

✅ **Готово! У тебя есть Animator с одним состоянием Walk**

---

### 3.2 Создай Enemy2_AnimatorController

**Повтори ШАГ 1-4 для второго врага:**
1. Создай файл: `Enemy2_AnimatorController`
2. Двойной клик → открой Animator
3. Создай состояние `Walk`
4. Motion: назначь `Enemy2_Walk` (анимацию второго врага)
5. Set as Layer Default State

---

## 🎯 ЭТАП 4: Обновление EnemyController.cs (добавить Animator)

### 4.1 Добавить поддержку Animator

**Файл**: `Scripts/Combat/Enemy/EnemyController.cs`

**Изменения**:

```csharp
[Header("Visuals")]
[Tooltip("SpriteRenderer для визуальных эффектов")]
[SerializeField] private SpriteRenderer spriteRenderer;

[Tooltip("Animator для анимаций врага")]
[SerializeField] private Animator animator;
```

**В Awake() добавить**:
```csharp
if (animator == null)
{
    animator = GetComponent<Animator>();
}
```

**В FixedUpdate() (или отдельный метод UpdateAnimation)**:
```csharp
// Обновление анимации
if (animator != null)
{
    bool moving = rb.linearVelocity.sqrMagnitude > 0.01f;
    // Если есть параметр isMoving - установить его
    // animator.SetBool("isMoving", moving);
    
    // Пока враги всегда движутся - анимация уже играет по умолчанию
}
```

**Flip спрайта** (поворот влево/вправо):
```csharp
private void UpdateVisuals()
{
    if (target == null || spriteRenderer == null) return;
    
    // Флип в зависимости от направления движения
    Vector2 direction = (target.position - transform.position).normalized;
    if (Mathf.Abs(direction.x) > 0.1f)
    {
        spriteRenderer.flipX = direction.x < 0;
    }
}
```

**Вызвать UpdateVisuals() в FixedUpdate()**

---

## 🎯 ЭТАП 5: Создание префабов врагов

### 5.1 Создай Enemy_Type1 Prefab

**ШАГ 1: Создай пустой объект**

1. В окне **Hierarchy** (список объектов слева) кликни **ПКМ**
2. **Create Empty**
3. Назови: `Enemy_Type1`
4. Нажми Enter

**ШАГ 2: Добавь Sprite Renderer**

1. **Кликни на Enemy_Type1** в Hierarchy (выделится)
2. Справа в **Inspector** кликни **Add Component**
3. Найди и добавь: **Sprite Renderer**
4. В Sprite Renderer настрой:
   - **Sprite**: кликни на кружочек → выбери `enemies/1/Walk_0` (первый кадр)
   - **Sorting Layer**: `Default`
   - **Order in Layer**: `0`

**ШАГ 3: Добавь Animator**

1. **Add Component** → найди **Animator**
2. В Animator:
   - **Controller**: перетащи `Enemy1_AnimatorController` или кликни на кружочек и выбери

**ШАГ 4: Добавь Rigidbody2D**

1. **Add Component** → **Rigidbody 2D**
2. Настрой:
   - **Body Type**: `Dynamic`
   - **Gravity Scale**: `0` (ноль!)
   - **Constraints**: поставь галочку на **Freeze Rotation Z**

**ШАГ 5: Добавь коллайдер**

1. **Add Component** → **Circle Collider 2D**
2. Настрой:
   - **Is Trigger**: ✓ (поставь галочку!)
   - **Radius**: `0.3` (или подбери под размер спрайта)

**ШАГ 6: Добавь Health Component**

1. **Add Component** → в поиске напиши `Health Component`
2. Настрой:
   - **Max Health**: `50`
   - **Start Health**: `50`

**ШАГ 7: Добавь Enemy Controller**

1. **Add Component** → `Enemy Controller`
2. Настрой:
   - **Move Speed**: `3`
   - **Sprite Renderer**: перетащи компонент Sprite Renderer из верхней части Inspector
   - **Animator**: должен подцепиться автоматически

**ШАГ 8: Добавь Enemy Damage On Contact**

1. **Add Component** → `Enemy Damage On Contact`
2. Настрой:
   - **Damage Per Hit**: `5`
   - **Damage Interval**: `0.5`
   - **Player Layer**: выбери `Player` из списка

**ШАГ 9: Сделай префаб**

1. Открой папку `Prefabs` в окне **Project**
2. **Перетащи** `Enemy_Type1` из **Hierarchy** в папку **Prefabs**
3. Появится файл `Enemy_Type1.prefab`
4. **Удали** `Enemy_Type1` из **Hierarchy** (ПКМ → Delete)

✅ **Префаб готов!**

### 5.2 Создай Enemy_Type2 Prefab

**Повтори ШАГ 1-9, но с другими настройками:**

1. Имя: `Enemy_Type2`
2. Sprite: `enemies/2/Walk_0` (первый кадр второго врага)
3. Animator Controller: `Enemy2_AnimatorController`
4. Health Component:
   - Max Health: `75` (сильнее первого)
   - Start Health: `75`
5. Enemy Controller:
   - Move Speed: `2.5` (медленнее)
6. Enemy Damage On Contact:
   - Damage Per Hit: `8` (больнее)
   - Damage Interval: `0.5`
7. Сделай префаб в папке `Prefabs`
8. Удали из Hierarchy

---

## 🎯 ЭТАП 6: Обновление Enemy Spawner / Wave System

### 6.1 Настроить префабы в EnemyPool

**ШАГ 1: Открой сцену**
1. **File** → **Open Scene**
2. Выбери свою игровую сцену

**ШАГ 2: Найди EnemyPool**
1. В окне **Hierarchy** (слева) найди объект **EnemyPool**
2. **Кликни на него один раз** (выделится)

**ШАГ 3: Настрой массив префабов**
1. Справа в **Inspector** найди компонент **Enemy Pool**
2. Найди поле **Enemy Prefabs**:
   - Сейчас там **Size: 0** или старый префаб
   - Измени **Size** на `2` (два типа врагов)
3. Появятся два слота:
   - **Element 0**: перетащи `Enemy_Type1` из папки Prefabs
   - **Element 1**: перетащи `Enemy_Type2` из папки Prefabs

**ШАГ 4: Проверь настройки**
- **Initial Pool Size Per Type**: `15` (по 15 врагов каждого типа в пуле)

**ШАГ 5: Сохрани сцену**
- **Ctrl+S** или **File** → **Save**

✅ **Теперь спавнер будет случайно выбирать между двумя типами врагов!**

---

## 🎯 ЭТАП 7: Настройка слоёв и коллизий

### 7.1 Проверка Layers

**Edit** → **Project Settings** → **Tags and Layers**:
- Layer 6: `Player`
- Layer 7: `Enemy`
- Layer 8: `Projectile`

**Назначить слои**:
- `Enemy_Type1` prefab → Layer: `Enemy`
- `Enemy_Type2` prefab → Layer: `Enemy`

### 7.2 Collision Matrix

**Edit** → **Project Settings** → **Physics 2D** → **Layer Collision Matrix**:

**Enemy vs Player**: ✓ (для триггера урона)
**Enemy vs Enemy**: ✗ (враги проходят друг через друга)
**Enemy vs Projectile**: ✓ (снаряды попадают во врагов)

---

## 🎯 ЭТАП 8: Тестирование

### 8.1 Визуальная проверка

1. **Play Mode**
2. **Проверить**:
   - ✅ Враги появляются с анимацией Walk
   - ✅ Анимация зациклена и плавная
   - ✅ Спрайт флипится при движении влево/вправо
   - ✅ Два типа врагов выглядят по-разному
   - ✅ Враги не застревают друг в друге

### 8.2 Функциональная проверка

1. **Атака врагов**:
   - ✅ Враги наносят урон при касании
   - ✅ Каждый враг атакует независимо (по 5/8 урона)
   
2. **Урон по врагам**:
   - ✅ Снаряды попадают и убивают врагов
   - ✅ AOE заклинания бьют всех в радиусе
   - ✅ Анимация смерти (если добавлена)

3. **Performance**:
   - ✅ Нет лагов при 10+ врагах на экране
   - ✅ Анимации работают плавно

---

## 🎯 ЭТАП 9: Опциональные улучшения

### 9.1 Добавить Idle анимацию

Если враги должны иногда стоять:
1. Создать `Enemy1_Idle.anim` (из `Idle.png`)
2. Добавить в Animator Controller:
   - State: `Idle`
   - Transition: `Idle ↔ Walk` (по параметру `isMoving`)
3. В EnemyController:
   ```csharp
   animator.SetBool("isMoving", rb.linearVelocity.sqrMagnitude > 0.01f);
   ```

### 9.2 Добавить Death анимацию

1. Создать `Enemy1_Death.anim` (из `Death.png`)
2. В Animator: добавить состояние `Death`
3. В HealthComponent.OnDeath():
   ```csharp
   animator.SetTrigger("death");
   ```
4. Death state → **не зацикленный** (Loop: ✗)
5. После смерти удалять объект через событие анимации

### 9.3 Добавить Hurt эффект

1. В HealthComponent.ApplyDamage():
   ```csharp
   // Flash эффект
   if (spriteRenderer != null)
   {
       StartCoroutine(FlashRed());
   }
   ```

2. Метод FlashRed:
   ```csharp
   private IEnumerator FlashRed()
   {
       spriteRenderer.color = Color.red;
       yield return new WaitForSeconds(0.1f);
       spriteRenderer.color = Color.white;
   }
   ```

---

## ✅ Чеклист выполнения

### Спрайты и анимации:
- [ ] Walk.png импортирован и нарезан (Enemy 1)
- [ ] Walk.png импортирован и нарезан (Enemy 2)
- [ ] Enemy1_Walk.anim создана (6 кадров, loop)
- [ ] Enemy2_Walk.anim создана (6 кадров, loop)

### Animator Controllers:
- [ ] Enemy1_AnimatorController создан (Walk state по умолчанию)
- [ ] Enemy2_AnimatorController создан (Walk state по умолчанию)

### Код:
- [ ] EnemyController.cs обновлён (Animator, UpdateVisuals)
- [ ] Flip спрайта работает корректно

### Префабы:
- [ ] Enemy_Type1 prefab создан и настроен
- [ ] Enemy_Type2 prefab создан и настроен
- [ ] Слои назначены правильно (Layer: Enemy)

### Spawning:
- [ ] EnemyWaveSO/Spawner обновлены с новыми префабами
- [ ] Оба типа врагов спавнятся корректно

### Тестирование:
- [ ] Анимация Walk играет постоянно
- [ ] Спрайты флипаются при движении
- [ ] Враги наносят урон игроку
- [ ] Снаряды и AOE убивают врагов
- [ ] Нет визуальных багов или лагов

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Анимация не играет
**Решение**:
- Проверь что Animator Controller назначен на префабе
- Убедись что Walk state - Default State (оранжевый)
- Проверь что в анимации есть keyframes (не пустая)

### Проблема 2: Спрайт не флипается
**Решение**:
- Убедись что `spriteRenderer` назначен в Inspector
- Проверь что UpdateVisuals() вызывается в Update/FixedUpdate
- Debug.Log направление движения

### Проблема 3: Враги накладываются друг на друга
**Решение**:
- Physics2D → Collision Matrix: Enemy vs Enemy = ✗
- Rigidbody2D → Body Type = Dynamic (не Kinematic!)
- Collider → Is Trigger = ✓

### Проблема 4: Снаряды не попадают
**Решение**:
- Проверь Layer врага (должен быть `Enemy`)
- Physics2D → Collision Matrix: Projectile vs Enemy = ✓
- Collider → Is Trigger = ✓

### Проблема 5: Плохая производительность
**Решение**:
- Используй Object Pooling для врагов
- Ограничь количество одновременных врагов (max 20-30)
- Используй спрайты с низким разрешением

---

## 📊 Результат

После выполнения Stage 13:
- ✅ Враги выглядят как полноценные персонажи, а не кружки
- ✅ Плавная анимация ходьбы (8-10 FPS)
- ✅ Два визуально разных типа врагов
- ✅ Правильный flip спрайта при движении
- ✅ Вся функциональность сохранена (урон, смерть, spawning)
- ✅ Готовность к расширению (можно добавить Death, Hurt, Attack анимации)

---

**Следующий этап**: Stage 14 - добавление разнообразия врагов (Attack, Death анимации, новые типы)
